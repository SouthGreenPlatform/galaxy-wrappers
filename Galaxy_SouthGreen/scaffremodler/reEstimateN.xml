<tool id="reEstimateN" name="reEstimateN" version="0.1"> 
    <description> : Re-estimate N in multifasta (N restimated are converted to S)</description>  
    <command>
		source $__tool_directory__/include_scaffremodler.sh ;
        $__tool_directory__/reEstimateN.py
		--config $config
		--exclude $exclude
		--min_read $min_read
		--out $out
		
    </command>
    <inputs>
		<param name="config" type="data" label="The conf file generated by the workflow after steps 1_create_conf to 4_filter_sam" />
		<param name="exclude" type="text" value="No_exclude" label="Sequence names separated with '=' to exclude from N re-estimation" />
		<param name="min_read" type="integer" value="30" label="The minimal read number requested to make the N estimation" />
		<param name="OUT" type="text" label="Identifier for output" />
    </inputs>
    <outputs>
        <data format="fasta" name="out" label="$OUT N_estimated" />
    </outputs>
    <help>
    </help>
</tool>  